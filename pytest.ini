[pytest]
testpaths = tests
addopts = -ra -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: end-to-end tests that bind real sockets / use TLS
    slow: tests that take noticeable time
